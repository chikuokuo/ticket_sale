# Flutter rules are compiled into the base Flutter Gradle script. By default,
# only add your own rules here. All other rules are in files starting with
# "flutter-proguard-".

-keep class com.stripe.android.** { *; }
-dontwarn com.stripe.android.**

